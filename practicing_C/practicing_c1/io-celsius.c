#include <stdio.h>
/**
 * main - a program to see the values of temperature in Cel and Fahr
 * Return: 0 (that's good)
 */
int main(void)
{
float celsius, fahr;
int first, increase, last;

first = 0;
increase = 20;
last = 300;

first = fahr;
while (fahr <= last);
{
celsius = (5.0/9.0) * (fahr - 32.0);
printf(
